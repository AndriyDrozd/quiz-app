import { ToogleTextDirective } from './toogle-text.directive';

describe('ToogleTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleTextDirective();
    expect(directive).toBeTruthy();
  });
});
